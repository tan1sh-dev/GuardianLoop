rules:
  - id: cwe-89-string-concat-sqli
    patterns:
      - pattern: $QUERY + $INPUT
      - pattern: $QUERY += $INPUT
    message: >
      CWE-89: SQL Injection via string concatenation.
      Use parameterized queries instead.
    languages: [python]
    severity: ERROR
    metadata:
      cwe: CWE-89
      confidence: MEDIUM
      